

## 线程和进程

线程：进程是操作系统能够进行运算调度的最小单位。他被包含在进程之中，是进程中的实际运作单位



进程：进程是程序执行的实体



多线程：简单理解应用软件中相互独立，可以同时运行的共同功能



## 并发和并行

并发：在同一时刻，有多个指令在单个cpu上交替执行

并行：在同一时刻，有多个指令在多个cpu上同时执行



## 多线程的实现方式

1. 继承Thread类的方式进行实现

   ```java
   package a01threadcase1;
   
   public class MyThread extends Thread{
       @Override
       public void run() {
           for (int i = 0; i <100; i++) {
               System.out.println(getName()+i);
           }
       }
   }
   
   ```

   ```java
   package a01threadcase1;
   
   public class ThreadDemo {
       public static void main(String[] args) {
           MyThread mt = new MyThread();
           MyThread mt2 = new MyThread();
   
           mt.setName("线程1");
           mt2.setName("线程2");
           //启动线程 mt.run()只是运行mt对象中的run方法
           mt.start();
           mt2.start();
       }
   }
   
   ```

   

2. 实现Runnable接口的方式进行实现

   ```java
   package a01threadcase2;
   
   public class MyRun implements Runnable{
       @Override
       public void run() {
           for (int i = 0; i < 100; i++) {
               //获取当前线程
               Thread t = Thread.currentThread();
               System.out.println(t.getName() +" "+ i);
           }
       }
   }
   
   ```

   ```java
   package a01threadcase2;
   
   public class ThreadDemo {
       public static void main(String[] args) {
           //创建MyRun的对象
           //表示多线程要执行的任务
           MyRun mr = new MyRun();
   
           //创建线程对象
           Thread t1 = new Thread(mr);
           Thread t2 = new Thread(mr);
   
           //设置线程的名称
           t1.setName("线程1");
           t2.setName("线程2");
   
           //开启线程
           t1.start();
           t2.start();
       }
   }
   
   ```

   

3. 利用Callable接口和Future接口方式实现

```java
package a01threadcase3;

import java.util.concurrent.Callable;

public class MyCallable implements Callable<Integer> {
    @Override
    public Integer call() throws Exception {
        //求1-100的和
        int sum = 0;
        for (int i = 0; i < 100; i++) {
            sum += i;
        }
        return sum;
    }
}

```

```java
package a01threadcase3;

import java.util.concurrent.ExecutionException;
import java.util.concurrent.FutureTask;

public class ThreadDemo {
    public static void main(String[] args) throws ExecutionException, InterruptedException {
        //特点：可以获得多线程运行结果
        //创建MyCallable的对象(表示多线程要执行的任务)
        MyCallable mc = new MyCallable();
        //创建FutureTask的对象(作用管理多线程运行的结果)
        FutureTask<Integer> ft = new FutureTask<>(mc);
        //创建线程对象
        Thread t1 = new Thread(ft);
        //启动线程
        t1.start();
        //获取多线程结果
        Integer i =  ft.get();
        System.out.println(i);

    }
}

```

## 常见的成员方法

![image-20260331113252463](C:\Users\18158\AppData\Roaming\Typora\typora-user-images\image-20260331113252463.png)



```java
package a01threadcase4;

public class ThreadDemo {
    public static void main(String[] args) throws InterruptedException {

        MyThread mt = new MyThread("飞机");
        MyThread mt2 = new MyThread("坦克");

        //设置守护线程
        //当非守护线程执行完毕，守护线程会自动结束(陆续结束)
        mt2.setDaemon(true);

        mt.start();
        mt2.start();

        //获取优先级
        System.out.println(mt.getPriority());
        System.out.println(mt2.getPriority());



        Thread t = Thread.currentThread();
        System.out.println(t.getName());
        System.out.println("主线开始");
        Thread.sleep(5000);
        System.out.println("主线结束");

        //插队
        mt.join();

        //执行在main线程中
        for (int i = 0; i < 10; i++) {
            System.out.println("main线程"+i);
        }
    }
}

```

```java
package a01threadcase4;

public class MyThread extends Thread{

    public MyThread(){
    }


    public MyThread(String name){
        super(name);
    }

    @Override
    public void run() {
        for (int i = 0; i <100; i++) {
            System.out.println(getName()+"@"+i);

            //表示出让当前CPU的执行权
            Thread.yield();
        }
    }
}

```



## 线程生命周期

![image-20260331142139378](C:\Users\18158\AppData\Roaming\Typora\typora-user-images\image-20260331142139378.png)

![344234ea28b9fdee116f0eba7413e04a_720](C:\Users\18158\Documents\Tencent Files\2675625005\nt_qq\nt_data\Pic\2026-03\Thumb\344234ea28b9fdee116f0eba7413e04a_720.jpg)

## 线程安全

同步代码块

```java
package a01threadcase5;

public class MyThread extends Thread{
    static int ticket = 0;

    //锁对象，一定是唯一的
    static final Object obj = new Object();
    @Override
    public void run() {
        while (true){
            //同步代码块
            synchronized (MyThread.class){
                if(ticket < 100){
                    try {
                        Thread.sleep(100);
                    } catch (InterruptedException e) {
                        throw new RuntimeException(e);
                    }
                    ticket++;
                    System.out.println(getName()+"正在卖第"+ticket+"张票");
                }else break;
            }
        }
    }
}

```

同步方法

```java
package a01threadcase6;

public class MyThread implements Runnable{
    int ticket = 0;
    @Override
    public void run() {
        while(true){
            if(method()) break;
        }
    }

    private synchronized boolean method(){
        if(ticket == 100) return true;
        else{
            try{
                Thread.sleep(10);
            }catch (InterruptedException e){
                e.printStackTrace();
            }
            ticket++;
            System.out.println(Thread.currentThread().getName()+"正在卖第"+ticket+"张票");
        }
        return false;
    }
}
```

synchronized默认打开，进入后会自动上锁，结束后自动释放锁

lock可以手动上锁，手动释放锁

```java
package a01threadcase7;

import java.util.concurrent.locks.Lock;
import java.util.concurrent.locks.ReentrantLock;

public class MyThread implements Runnable{
    static int ticket = 0;
    static Lock lock = new ReentrantLock();

    @Override
    public void run() {
        while (true){
            lock.lock();
            try{
                if(ticket < 100){
                    try {
                        Thread.sleep(100);
                    } catch (InterruptedException e) {
                        throw new RuntimeException(e);
                    }
                    ticket++;
                    System.out.println(Thread.currentThread().getName()+"正在卖第"+ticket+"张票");
                }else break;
            }finally {
                lock.unlock();
            }
        }

    }
}

```

## 生产者和消费者

1. 等待唤醒机制

   ```java
   package a01threadcase8;
   
   public class Cook extends Thread{
       @Override
       public void run() {
           while (true){
               synchronized (Desk.lock) {
                   if (Desk.remainToMake == 0) {
                       Desk.lock.notifyAll();
                       break;
                   }
                   if (Desk.foodFlag == 1) {
                       try {
                           Desk.lock.wait();
                       } catch (InterruptedException e) {
                           throw new RuntimeException(e);
                       }
                   } else {
                       System.out.println("厨师做了第 " + (Desk.totalFood - Desk.remainToMake + 1) + " 个食物");
                       Desk.foodFlag = 1;
                       Desk.remainToMake--;
                       Desk.lock.notifyAll();
                   }
               }
           }
       }
   }
   
   ```

2. 阻塞队列

   ```java
   package a01threadcase9;
   
   import java.util.concurrent.ArrayBlockingQueue;
   
   public class Cook extends Thread{
       ArrayBlockingQueue<String> queue;
       public Cook(ArrayBlockingQueue<String> queue){
           this.queue = queue;
       }
       @Override
       public void run(){
           while (true){
               try {
                   queue.put("面条");
                   System.out.println("厨师做了一碗面");
               } catch (InterruptedException e) {
                   throw new RuntimeException(e);
               }
           }
   
       }
   }
   
   ```

   