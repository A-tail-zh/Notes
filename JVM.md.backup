# JVM



![image-20260305154051146](C:\Users\18158\AppData\Roaming\Typora\typora-user-images\image-20260305154051146.png)

## 程序计数器（pc）

作用：记住下一条jvm指令的执行地址

特点：是线程私有的

​	    不会存在内存溢出



## 虚拟机栈

线程运行所需要的内存空间

有栈帧组成

栈帧：每个方法运行时的空间

每个线程只能有一个活动栈帧对应着当前正在运行的那个方法



问题

1.垃圾回收是否涉及栈内存

不涉及，垃圾回收机制是为了解决堆内存中对象生命周期无法在编译时确定的问题而设计的。而栈内存随方法调用而生，随方法结束而亡，它的清理是确定性的，由操作系统或运行时直接完成。

2.栈内存分配越大越好吗

不是，栈内存的大小需要根据应用的具体场景（是计算密集型、递归型还是高并发型）来平衡，盲目追求“大”往往会浪费宝贵的系统内存资源。

3.方法内的局部变量是线程安全的吗

如果方法内局部变量没有逃离方法的作用访问，它是线程安全的

如果是局部变量引用了对象，并逃离了方法的作用方法，需要考虑线程安全



栈内存溢出

1.栈帧过多导致栈内存溢出：递归调用

2.栈帧过大导致内存溢出



线程运行诊断

1.cpu占用过多

用top定位哪个进程对cpu的占用过高

```shell
ps H -eo pid,tid,%cpu|grep 进程id
```

用ps命令进一步定位哪个线程引起的cpu占用过高

jstack进程id,可以根据线程id找到有问题的线程，进一步定位到问题代码的源码行号

2.迟迟得不到结果

发生了死锁，排查方法同上



## 本地方法栈

本地方法：使用native关键字修饰的Java方法

本地方法栈是JVM中专门为本地方法（native方法）执行而开辟的内存区域



## 堆

通过new创建的对象都会使用堆内存

特点：

线程共享的，堆中对象都需要考虑线程安全

有垃圾回收机制

堆空间溢出：未回收一直使用



堆内存诊断：

1.jps工具

​	查看当前系统中有哪些java进程

2.jmap工具

​	查看堆内存占用情况，只能监测时刻

```shell
jmap -heap 进程id
```

3.jconsole工具

​	图形界面定位，多功能的监测工具，可以连续监测



垃圾回收后，内存占用还是很高 jvisualvm 堆转储 dump



# 方法区

方法区内存溢出：元空间(使用的是系统资源)内存溢出

场景：spring mybatis



常量池，就是一张表，虚拟机指令根据这张常量表找到要执行的类名、方法名、参数类型、字面量等信息

运行时常量池，常量池时*.calss文件中的，当该类被加载，它的常量池信息就会放入运行时常量池，并将里面的符号地址变为真实地址



常量池中的信息，都会被加载到运行时常量池中，这时 a ，b， ab 都是常量池中的符号，还没有变为 java 字符串对象

ldc #2(使用时) 会把 a 符号变为 "a" 字符串对象

可以使用intern方法，主动将还未放入串池的字符串放入串池

StringTable转移到Heap

StringTable也会发生垃圾回收

`StringTable` 底层是一个固定大小的哈希表，它不像 `HashMap` 那样可以自动扩容。这个表由一个个“桶”（Bucket）组成。当不同的字符串计算出的哈希值落到同一个桶里时，就会形成链表，导致查找速度变慢

调优的第一个手段就是**增加桶的数量，以减少哈希冲突**。

调优的第二个手段StringTable天然就是一个复用池，我们可以主动利用它来**消除重复，大幅节约内存**

## 直接内存

不受垃圾回收管理

传统内存

![image-20260309102931140](C:\Users\18158\AppData\Roaming\Typora\typora-user-images\image-20260309102931140.png)

引入直接内存

![image-20260309103057478](C:\Users\18158\AppData\Roaming\Typora\typora-user-images\image-20260309103057478.png)

直接内存释放

使用了unsafe对象完成直接内存的分配，并并且回收需要主动调用freeMemory方法

ByteBuffer的实现类内部，使用了cleaner（虚引用）来监测ByteBuffer对象，一旦ByteBuffer对象被垃圾回收,那么就会有ReferenceHandler线程通过Cleaner的clean方法调用freeMemory来释放直接内存



## 垃圾回收



引用计数法

可达性分析法（java）**可达性分析法**是一种通过一系列称为 **GC Roots** 的对象作为起点，从这些起点开始向下搜索引用链，判断对象是否存活的算法。

1. 栈帧中的本地变量表引用的对象
2. 方法区中静态属性引用的对象
3. 方法区中常量引用的对象
4. 本地方法栈中 JNI 引用的对象
5. Java 虚拟机内部的引用
6. 所有被同步锁（synchronized）持有的对象

引用：

强引用：

软引用：内存不够才回收

弱引用：不管内存直接回收

下面必须配合引用队列：

虚引用：

终结器引用：

| 引用类型       | 回收时机                    | 主要用途                 | 是否配合引用队列 |
| :------------- | :-------------------------- | :----------------------- | :--------------- |
| **强引用**     | 永不回收（除非 Roots 断开） | 普通对象                 | 否               |
| **软引用**     | 内存不足时回收              | 缓存实现                 | 可选             |
| **弱引用**     | 下次 GC 就回收              | WeakHashMap、ThreadLocal | 可选             |
| **虚引用**     | 任何时候都可能回收          | 对象回收跟踪             | **必须**         |
| **终结器引用** | Finalize 时                 | 对象销毁前的资源释放     | **必须**         |

垃圾回收算法

1.标记清楚

优点：速度快

缺点：内存碎片

2.标记整理

优点：没内存碎片

缺点：速度慢

3.复制

先标记，把存活的复制，再交换

优点：没内存碎片

缺点：双倍内存空间

分代回收

新生代(Minor GC)：伊甸园，幸存区From，幸存区To.

老年代(Full GC)：

过程：

- 对象首先分配在伊甸园区域
- 新生代空间不足时，触发 `minor gc`，伊甸园和 `from` 存活的对象使用 `copy` 复制到 `to` 中，存活的对象年龄加1并且交换 `from to`
- `minor gc` 会引发 `stop the world`，暂停其它用户的线程，等垃圾回收结束，用户线程才恢复运行
- 当对象寿命超过阈值时，会晋升至老年代，最大寿命是15（4bit）
- 当老年代空间不足，会先尝试触发 `minor gc`，如果之后空间仍不足，那么触发 `full gc`，STW的时间更长

子线程出现内存溢出不会影响主线程的运行（X）

### 总结对比

| 内存溢出类型                         | 影响范围 | 是否影响主线程   |
| ------------------------------------ | -------- | ---------------- |
| **堆内存溢出** (Heap OOM)            | 整个 JVM | ✅ **会影响**     |
| **方法区溢出** (Metaspace OOM)       | 整个 JVM | ✅ **会影响**     |
| **栈溢出** (StackOverflow)           | 单个线程 | ❌ **通常不影响** |
| **直接内存溢出** (Direct Memory OOM) | 整个 JVM | ✅ **会影响**     |

大对象在新生代内存不够时，会直接进入老年代



垃圾回收器

串行：

单线程

堆内存较小，适合个人电脑

吞吐量优先：

多线程

堆内存较大，多核线程

让单位时间内，stw的时间最短

响应时间优先：

多线程

堆内存较大，多核cpu

单次stw时间尽可能最短

G1

适用场景：

同时注重吞吐量和低延迟，默认的暂停目标是200ms

超大堆内存，会将堆划分为多个大小相等的Region

整体上是标记+整理算法，两个区域之间是复制算法

SerialGC（串行收集器）

新生代垃圾收集器：Serial（复制算法）

触发 **Minor GC** 的条件：

- ✅ Eden 区空间不足

老年代垃圾收集器：Serial Old（标记-整理算法）

触发 **Full GC** 的条件：

- ✅ 老年代空间不足
- ✅ Metaspace（方法区）空间不足
- ✅ 显式调用 `System.gc()`
- ✅ Minor GC 后存活对象放不进 Survivor 和老年代

ParallelGC（并行收集器）

新生代垃圾收集器：Parallel Scavenge（复制算法）

触发 **Minor GC** 的条件：

- ✅ Eden 区空间不足

老年代垃圾收集器：Parallel Old（标记-整理算法）

触发 **Full GC** 的条件：

- ✅ 老年代空间不足
- ✅ Metaspace 空间不足
- ✅ 显式调用 `System.gc()`
- ✅ 空间分配担保失败

CMS（Concurrent Mark Sweep）

新生代垃圾收集器：ParNew（复制算法）

触发 **Minor GC** 的条件：

- ✅ Eden 区空间不足

老年代垃圾收集器：CMS（标记-清除算法）

**CMS 有两种 GC**：

1. CMS 并发 GC（不是 Full GC）

触发条件：

- ✅ 老年代使用率达到阈值（默认 92%）

bash

```bash
  -XX:CMSInitiatingOccupancyFraction=92
```

2. Full GC（Concurrent Mode Failure）

触发条件：

- ✅ **老年代空间不足**（CMS GC 期间晋升失败）
- ✅ **Metaspace 空间不足**
- ✅ **Promotion Failed**（新生代晋升失败）
- ✅ **Concurrent Mode Failure**（并发失败）
- ✅ 显式调用 `System.gc()`

G1（Garbage First）

新生代和老年代：G1（混合收集）

1. Young GC（新生代回收）

触发条件：

- ✅ Eden 区空间不足

2. Mixed GC（混合回收）

触发条件：

- ✅ 老年代占用空间达到阈值（默认 45%）

bash

```bash
  -XX:InitiatingHeapOccupancyPercent=45
```

3. Full GC（完全回收）

触发条件：

- ✅ **Mixed GC 无法跟上对象分配速度**
- ✅ **老年代空间耗尽**
- ✅ **Humongous 对象分配失败**（超大对象）
- ✅ **Metaspace 空间不足**
- ✅ 显式调用 `System.gc()`



GC调优

新生代调优：

新生代特点：1.所有new操作的内存分配非常廉价2.死亡对象的回收代价是零3.大多数对象用过即死4.MinorGC的时间远远低于FullGc





